// PostgreSQL-only document summary/pipeline/read compatibility boundary.
// Explicit Zoho mutations remain on their separately authorized action routes.
export { dynamic, GET } from '../zoho-invoices/route'
