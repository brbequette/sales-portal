# Sales lifecycle blocker audit

Status: authoritative mapping and task acceptance released and production-verified; communication, financial-document, payment, and fulfillment acceptance remain gated.

## Confirmed root causes

- Portal lead creation used a local placeholder as though it were a Zoho identifier and performed no CRM create.
- Conversion created invented account/contact identifiers, skipped locally created leads, ignored provider result status and IDs, and reported local success after provider failure.
- Unified delta sync discarded final batches smaller than 50, fetched only the first 200 records, advanced checkpoints after partial work, parsed CRM 204 as JSON, and used a non-atomic concurrency check.

## Implemented safeguards

- Durable provider operation keys make lead create and conversion idempotent.
- CRM, Books, and local identifiers have separate columns; legacy values are not guessed into them.
- Provider HTTP and per-record results are validated. Pending, syncing, successful, failed, and ambiguous outcomes are explicit.
- Timeout after submission becomes ambiguous. Lead creation performs an exact email/company lookup before retry; conversion requires reconciliation and is not automatically resent.
- Sync batches flush completely. Provider pages are bounded and resumable with a durable page pointer. The original timestamp checkpoint advances only after complete persistence; unresolved dependencies keep the current page recoverable.
- GitHub CI uses an isolated PostgreSQL 16 service to apply the full migration chain twice and rehearse an upgrade from the preceding schema. The first PR #95 run passed both database gates.
- Books customer creation now has one durable operation per local Account. It persists the returned Books customer ID, reconciles by exact stored CRM Account linkage, never matches by company name alone, and does not automatically resend an ambiguous POST.
- Order submission retains Books catalog item IDs. The authoritative gift hat (`1254360000043727500`) is selectable even when its local gift flag is stale, while administrative lines are excluded from merchandise and gift choices.
- Quote and sales-order POSTs use a caller-stable request UUID and an atomic durable operation claim. Provider acceptance is recorded before local persistence; an interruption in that window becomes ambiguous and ordinary retries cannot create another Books document.
- Read-only production evidence: DADGR458S is active and priced at $0.45, but Books reports a $0 purchase rate and no authoritative dropship flag, so cost/dropship acceptance remains blocked rather than guessed. Hat item `1254360000043727500` is active with a $0 sales rate, $20 purchase rate, and inventory tracking. The additive migration stores Books item IDs, purchase cost, and dropship eligibility explicitly, and the verified hat remains selectable if a bounded catalog response omits it.
- Missing product cost now blocks order selection. Numeric zero remains blocked unless its quality is explicitly `VERIFIED_ZERO`; it is never silently converted into zero-cost profit. Dropship eligibility remains nullable/unknown unless provider evidence satisfies the portal's purchase-order contract.
- Bounded alternative-product discovery chose RFD-50A060 for acceptance: active, $0.91 selling price, authoritative $0.36 purchase rate, purchase-capable non-inventory goods classification, and exact active preferred vendor `CONTINENTAL ABRASIVES`. Zoho Books does not document an item-level dropship eligibility flag; the portal therefore requires an audited application-managed business authorization. Zero stock and vendor presence alone remain insufficient.

## Follow-up acceptance blocker repair

- `/admin/lifecycle-reconciliation` and its POST-only administrator APIs reconcile an already-converted local Account/Lead pair by immutable local linkage. They use the durable CRM create/convert operations, never create another local Account, never resolve by company name alone, and never create a second Books customer.
- Existing Books customers now receive a targeted customer GET and persist the unique contact-person ID only when it matches the local primary contact by exact email or normalized phone. Ambiguous/missing matches fail closed.
- Task creation resolves the submitted local Account ID first, requires its authoritative `crmAccountId`, submits that ID as CRM `What_Id`, and persists the Task against the same local Account. Provider codes/messages remain readable instead of becoming `[object Object]`.
- RFD-50A060 product reconciliation is read-only upstream and idempotent locally. It verifies exact identity, active state, positive sales/purchase rates, purchase-capable goods type, non-inventory mode, and the exact active preferred vendor before persisting authoritative cost evidence.
- Gift selection retains the exact Books item `1254360000043727500` with $0 sales price and $20 authoritative cost; focused regression coverage remains in place.
- Production acceptance found two provider-shape mismatches: a locally converted Lead must not be created upstream with the local-only terminal `Converted` status, and Books may return an inventory account while explicitly setting `track_inventory` false. The hotfix maps only that terminal status to `New Lead` for the create-before-convert step and honors the explicit tracking boolean. Neither an inventory account nor zero stock authorizes dropship.
- CRM diagnostics identified the remaining rejected nested field as `id`, originating from the legacy `User.zohoId` submitted as `Owner.id`. Because that value is not an authoritative CRM Users mapping, the create payload now also omits `Owner`; local ownership remains unchanged and CRM applies its configured default until a dedicated CRM-user mapping exists. The payload continues to omit unverified optional `Lead_Status`, `Industry`, and `Time_Zone` fields.

## Remaining acceptance gates

- Provider-response tests reject HTTP errors, per-record errors, malformed bodies, and missing IDs. Durable create/convert paths preserve explicit syncing, failed, and ambiguous outcomes; broader end-to-end concurrency and transaction-interruption simulation remains a follow-up where not already covered by focused unit contracts.
- The restricted reconciliation path was deployed and invoked for local TEST Account `cmug0bjgs0002z35hikn7od9w` and Lead `cmufz988p00012v293aj45jas`. CRM Lead/Account/Contact and Books Customer/Contact mappings all verify as authoritative 19-digit identifiers. Exactly one CRM-backed Task was created against the same local Account.
- Production preflight confirms the approved contact controls and address, zero prior financial-document spend, and authoritative RFD-50A060 price/cost/vendor evidence. The business owner has now approved vendor dropshipping generally and specifically confirmed the verified RFD vendor. Pioneer shipments to California are the explicit exception and must route through Titan.
- Existing authorization covers the named TEST account, Ben-only communications, bounded sub-$5 purchase plus exact gift, documents/payment/reminder/campaign/PO/package/shipment/label, $100 cumulative cap, and secure user-entered card handoff. Action-specific safety checks still apply; no blanket reauthorization gate should be introduced.
- The repaired direct-dropship path must be released before financial acceptance: exact vendor/product mapping, audited eligibility, authoritative cost, Pioneer/California exception, stable request UUID, atomic provider claim, and ambiguous-outcome quarantine are mandatory. No financial or communication transaction was sent during this code repair.
- PR #105 is production merge `886c3f5ce75140211fdc54ec813358572a40e0cb`; Netlify deploy `6ab612447533790008a8dcd7` is ready and published that exact commit.
- The dropship safety follow-up is production merge `0d8fe768fe421646359a0f21bcfd8683939e9c2a` (PR #106), deployed as Netlify production deploy `6ab61f2fd7716b0008cd1ca2`. RFD-50A060 now has one audited `canDropship=true` authorization for exact vendor `CONTINENTAL ABRASIVES`; the Pioneer/California direct-dropship prohibition remains enforced. The guarded authorization made no Zoho call and created no document.
- Read-only production acceptance after that release confirms exact CRM/Books Account and primary-Contact mappings, the approved Centralia address, one pre-existing CRM-backed Task, zero Quotes/Sales Orders/Invoices/Payments/POs/Packages, and $0 cumulative observed document/shipping spend.
- Browser inspection found that `/sales/pos` was not using the shared submission path correctly: the standalone wrapper fetched only 100 alphabetically sorted products, did not pass `accountId`, and rendered a fake submit control whose only effect was a success toast. This is a release blocker for POS acceptance, not a provider failure. The follow-up repair removes that control, delegates preview/confirmation/submission to the shared idempotent builder, adds bounded authenticated product search, uses the immutable local Account ID, and presents separate CRM and Books mappings.

## Browser-retest repair follow-up

- PR #108 is deployed at merge `f7ae55df09a8ce5a00ea647c5e60ef6a85190625` / Netlify deploy `6ab647a19b05db0008ab9d98`. Its first exact TEST-account reconciliation exposed an independent sequencing defect: Books contact-person persistence gated address repair, and an exception escaped as a non-JSON HTTP 500. The follow-up queries the documented `/contacts/{contact_id}/contactpersons` endpoint when needed, permits address repair independently of contact-person persistence, writes durable stage evidence before provider mutations, verifies existing provider state before resubmission, and returns structured errors.
- Exact post-release provider inspection verified Books reconciliation and CRM billing/shipping address persistence. CRM exposes the customer timezone custom field as `Customer_Time_Zone`; the prior `Time_Zone` key was accepted but ignored. The final correction writes and verifies the authoritative API field and reuses the failed operation only after GET evidence proves the address portion already completed.

- The existing catalog `giftItem` checkbox controls the Gift shortcut list, while the ordinary 20%-of-available-profit limit remains enforced. An exact Books-mapped gift can receive an administrator-only, idempotent exception with an `OperationalAction` audit record. The exception preserves the $0 sales rate and real cost; unknown costs remain blocked.
- Product Information stores application-managed gift tags, size choices, an `IMMEDIATE`/`PAID_IN_FULL` release rule, and an in-app bundle definition. Fixed components use exact Product IDs; variable components resolve exact Books-mapped, authoritative-cost products by a configured tag. POS presents image-led searchable cards, calculates bundle cost from components, requires an exact variable product/size choice, and the transaction handler revalidates that choice before retaining it in the fulfillment plan.
- Product search results are enriched with historical sold quantity from stored line items. After each functional sort/filter, products with sales history rank ahead of never-sold products. Titan signature blade families have a dedicated visual recommendation rail and remain the first blade recommendations.
- Books Street1 now maps to the provider's `address` field. The exact-account reconciliation action updates and verifies both Books billing/shipping Street1 and CRM billing/shipping/timezone through provider APIs, then records the verified evidence locally.
- The POS cart requires fulfillment per paid line before submission, permits dropship only from explicit `canDropship=true`, displays the audited vendor, and stores the reviewed fulfillment plan with the local document payload. Complete addresses and cents-accurate COGS are shown; unresolved shipping/tax remain visibly pending rather than being represented as zero.
- Account SMS opens the account-linked composer after the tab change. The send surface uses a caller-stable request UUID and durable provider operation so in-progress, failed, or ambiguous outcomes cannot be silently resubmitted. The complete sanitized provider result is displayed.
- The POS empty state is withheld until its account search has actually completed.
- Eligibility remains fail-closed: a shortcut requires an active, non-administrative catalog row, an authoritative Books item ID, and authoritative positive cost or an explicit `VERIFIED_ZERO` classification. Selecting a gift sets its sales price to $0 while retaining authoritative cost in profit calculations.
- The known Titan hat Books item `1254360000043727500` remains an exact-ID fallback when bounded catalog data omits it or its local gift flag is stale. Its audited exception is an application-only setting and does not write the product to Zoho or create a financial document.
- The order preview shows the full available billing/shipping address and reviewed per-line fulfillment. Shipping remains explicitly not calculated and tax provider-calculated; the displayed amount remains labeled as a document subtotal rather than masquerading as a final landed total.
- After a successful document creation, POS retains the selected Account, shows the exact returned document number, and can open the established document action panel without leaving POS. Conversion, invoice, payment, purchase order, package, and label actions reuse their existing confirmation/audit paths; additional shortcuts cover fulfillment/shipping, follow-up tasks, and account work.
